# キャッシュの保存場所と設定を定義
proxy_cache_path /var/cache/nginx levels=1:2 keys_zone=llm_cache:10m max_size=1g inactive=10m use_temp_path=off;

server {
    listen 80;
    server_name localhost;

    # ▼▼▼【追加】/static/ 以下のリクエストを処理するlocationブロック ▼▼▼
    # このブロックにはキャッシュの設定を記述しない
    location /static/ {
        # バックエンドへの転送設定のみを記述
        proxy_pass http://api:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # ▼▼▼【変更】上記以外のパス("/")がこのブロックで処理される ▼▼▼
    location / {
        # --- キャッシュ設定 ---
        proxy_cache llm_cache;
        proxy_cache_key "$scheme$request_method$host$request_uri";
        proxy_cache_valid 200 10m; # 200 OKレスポンスを10分間キャッシュ
        proxy_cache_valid any 1m;  # その他のレスポンスは1分間キャッシュ

        # キャッシュのヒット/ミス状況を確認するためのヘッダーを追加
        add_header X-Proxy-Cache $upstream_cache_status;

        # --- バックエンドへの転送 ---
        proxy_pass http://api:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
